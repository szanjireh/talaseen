# Environment Variables Template for GitHub Actions

# Copy this file and fill in your values when setting up GitHub Secrets

# ============================================
# REQUIRED SECRETS - Docker Hub
# ============================================

DOCKER_USERNAME=your_dockerhub_username
# Example: johndoe

DOCKER_PASSWORD=your_dockerhub_access_token
# Example: dckr_pat_xxxxxxxxxxxxxxxxxxxx
# Generate at: https://hub.docker.com/settings/security


# ============================================
# OPTIONAL SECRETS - Server Deployment
# ============================================

SERVER_HOST=your_server_ip_or_domain
# Example: 192.168.1.100 or example.com

SERVER_USER=your_ssh_username
# Example: ubuntu, root, admin

SERVER_PATH=/path/to/project/on/server
# Example: /home/ubuntu/talaseen

SSH_PRIVATE_KEY=your_ssh_private_key
# Full private key including:
# -----BEGIN OPENSSH PRIVATE KEY-----
# ...key content...
# -----END OPENSSH PRIVATE KEY-----
# Generate with: ssh-keygen -t ed25519 -C "github-actions"


# ============================================
# PRODUCTION SERVER .env FILE
# ============================================
# These should be set on your production server
# in the .env file and services/backend/.env file

# Root .env file:
POSTGRES_USER=talaseen
POSTGRES_PASSWORD=your_secure_postgres_password
POSTGRES_DB=talaseen

# services/backend/.env file:
DATABASE_URL="postgresql://talaseen:your_secure_postgres_password@postgres:5432/talaseen"
JWT_SECRET=your_very_secure_jwt_secret_key_here
PORT=4000
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_CALLBACK_URL=https://yourdomain.com/api/auth/google/callback


# ============================================
# DOCKER HUB REPOSITORY NAMES
# ============================================
# Create these repositories on Docker Hub before running CD pipeline

# Backend repository: {DOCKER_USERNAME}/talaseen-backend
# Frontend repository: {DOCKER_USERNAME}/talaseen-frontend


# ============================================
# NOTES
# ============================================

# 1. Never commit actual secrets to Git
# 2. Use strong, unique passwords
# 3. Rotate secrets regularly
# 4. Use access tokens instead of passwords where possible
# 5. Keep backup of secrets in secure location (password manager)
# 6. Test secrets locally before adding to GitHub
# 7. Use different secrets for staging and production


# ============================================
# TESTING SECRETS LOCALLY
# ============================================

# Test Docker Hub credentials:
# echo "DOCKER_PASSWORD" | docker login -u DOCKER_USERNAME --password-stdin

# Test SSH connection:
# ssh -i /path/to/private/key SERVER_USER@SERVER_HOST

# Test Docker Hub repositories:
# docker pull DOCKER_USERNAME/talaseen-backend:latest
# docker pull DOCKER_USERNAME/talaseen-frontend:latest
